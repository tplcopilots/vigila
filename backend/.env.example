PORT=8080
API_KEY=change-me-api-key
REQUEST_SIGNING_SECRET=change-me-signing-secret
SIGNATURE_MAX_SKEW_MS=300000

# fallback active provider if client does not pass provider in request
ACTIVE_STORAGE_PROVIDER=firebase

# storage folders
STORAGE_ROOT=./storage

# oneDrive settings
ONEDRIVE_ACCESS_TOKEN=
ONEDRIVE_FOLDER=vigilance-videos

# firebase settings
FIREBASE_STORAGE_BUCKET=
# Path to service account json file
GOOGLE_APPLICATION_CREDENTIALS=
